package com.Sadiq;

import java.io.ObjectInputStream.GetField;
import java.util.Arrays;
import java.util.Collections;
import java.util.Iterator;
import java.util.List;
import java.util.stream.Collectors;
import org.apache.commons.lang3.StringUtils;


public class PAssign {

	public static void main(String[] args) {
		
		List<String> strList = Arrays.asList("abc","","bcd","","defg","");
		
//	System.out.println(	strList.stream().filter(t->t=="").count());
   //  System.out.println(strList.stream().StringUtils.filter(t->t.startsWith("a")).distinct().count());  
	//	System.out.println(	strList.stream().filter(s->s!="").collect(Collectors.toList())); 
	}

}
