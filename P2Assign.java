package com.Sadiq;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.stream.Collectors;
import java.util.stream.Stream;

public class P2Assign {
	
public static void main(String[] args) {
	//List<Integer> numbers = Arrays.asList(9,10,3,4,7,3,4);
	
	//System.out.println(numbers.stream().map(t->t*t).distinct().collect(Collectors.toList()));

//	List<String> cities = Arrays.asList("London","HongKong","Paris","NewYork");
//	
//	List<String> City = cities.stream().map(t->t.toUpperCase()).collect(Collectors.toList());
//	
//	System.out.println(City);

	
	
	
}
}
